# @openref/search

## 0.1.0

### Minor Changes

- The first published version. Everything the READMEs, the llms texts and https://openref.dev describe: the reference engine, the runtime collectors, the themes and the theme authoring kit, the runner, the CLI and the static build, at one version shared by every package.
